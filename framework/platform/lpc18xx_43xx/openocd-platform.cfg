proc program_and_run_old { exec_goal_name } {
   halt 0
   echo "**** writing $exec_goal_name "
   flash write_image erase $exec_goal_name 0x1A000000 elf
   reset run
   shutdown
}

proc program_and_run { exec_goal_name } {
   program $exec_goal_name verify reset exit
}
